using formula - L(Wi+bi) = 1/n Σ L(yi, f(xi) ) + \(\alpha \)R(Wi)   -> for Regularization

for n numbers of inputs: L = 1/n Σ Max(0, -yi f(xi) )
f(xi) = WiXi + b


Gradient Decent for optimal getting optimal weights
 Wi = Wi - η dL/dWi
 b = b - η dl/db
