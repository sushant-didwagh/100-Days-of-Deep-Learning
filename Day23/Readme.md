Regularization - technuque used to reduce overfitting 
doen by adding extra penalty of original loss(cost) function nothing but positive regularization factor
three types L1(penalty lambda/2n * W), L2 (prnalty lambda/2n * w^2) and L3 =L1 + L2

Wnew = (1-lambda)Wo - leraning_rate Loss derivated by weights
